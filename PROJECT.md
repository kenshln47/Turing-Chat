"# Project: Turing Chat Markdown & Syntax Highlighting\n\n## Architecture\nThe monorepo consists of:\n- `@turing-chat/core` (`packages/core`): Core agent state machine and engine.\n- `@turing-chat/react` (`packages/react`): React UI components. Contains `MessageBubble` where raw content needs markdown rendering.\n- `@turing-chat/nextjs` (`packages/nextjs`): Next.js integration helper.\n- `examples/nextjs-chat` (`examples/nextjs-chat`): Next.js app showcasing components.\n\nData flow:\n1. `MessageBubble` accepts a `message` object with `content` (string).\n2. Instead of standard regex segmentation (`parseMarkdown`), it should parse the content as Markdown via `react-markdown` and render it as HTML elements.\n3. Code blocks should be rendered using `react-syntax-highlighter` instead of simple `<pre><code>`.\n4. Code blocks should also feature a header with a \"Copy Code\" button to copy code content to the clipboard.\n\n## Code Layout\n- `packages/react/src/components/MessageBubble.tsx` — Main chat message bubble component.\n- `packages/react/package.json` — Package configuration for React UI package.\n- `examples/nextjs-chat` — Next.js sample chat app for visual validation.\n\n## Milestones\n| # | Name | Scope | Dependencies | Status | Conversation ID |\n|---|------|-------|--------------|--------|-----------------|\n| E2E | E2E Testing Suite | Set up testing infrastructure (Vitest + React Testing Library) and write comprehensive test cases covering Tiers 1-4. Publish `TEST_READY.md`. | None | PLANNED | |\n| 1 | Dependency Setup & Markdown rendering | Add `react-markdown` and its peer dependencies. Update `MessageBubble` to parse and render standard Markdown elements (R1). | None | PLANNED | |\n| 2 | Syntax Highlighting | Add `react-syntax-highlighter`. Render code blocks using syntax highlighting styles (R2). | M1 | PLANNED | |\n| 3 | \"Copy Code\" Button | Add \"Copy Code\" button in the header of code blocks, copying string to clipboard, indicating success visually (R3). | M2 | PLANNED | |\n| 4 | Final E2

<truncated 586 bytes>